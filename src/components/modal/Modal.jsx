import React from "react";
import { StyledModal } from "./modalStyle";
const Modal = ({}) => {
  return (
    <StyledModal>
      <button>끄기</button>
    </StyledModal>
  );
};

export default Modal;
